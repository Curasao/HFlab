Файл - parser.py

Полученный результат - 16млн вхождений буквы а и 16 млн строк

Логика выполнения: с помощью библиотеки gdown скачала файл с google drive,
далее распаковала с помощью библиотеки zipfile, посчитала строки и вхождения буквы а в файле.

Затраченное время: 8 часов
